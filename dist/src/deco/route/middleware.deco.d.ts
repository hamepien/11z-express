/// <reference path="../api/middleware.deco.d.ts" />
/**
 * Referred to ./api/middleware.
 *
 * We don't need any codes here and you don't have to worries.
 * All logic are inside the ./api/middleware.deco folder. everything just working fine!
 */
export {};
